import { Styledprops } from '@/types';
import { RecuitBtn } from '../../../../components/header/CHeader.styled';

export default function RemoveBtn(props: Styledprops) {
  return <RecuitBtn {...props}>삭제</RecuitBtn>;
}
