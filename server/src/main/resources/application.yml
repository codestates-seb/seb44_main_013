spring:
  datasource:
    url: jdbc:mysql://localhost:3306/portfolly
    driver-class-name: com.mysql.cj.jdbc.Driver
    username:
    password:

  jpa:
    properties:
      hibernate:
        format_sql: true
        hbm2ddl:
          auto: create
    show-sql: true
